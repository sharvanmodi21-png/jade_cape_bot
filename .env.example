# Copy to .env for local runs. On Railway, set these as Variables instead.
TRADING_MODE=PAPER
SYMBOL=BTCUSDT
PAPER_START_EQUITY=10000
# LIVE trading is gated and intentionally NOT enabled by these alone.
# Do NOT set the two below unless you have fully tested and accept the risk:
# BINANCE_API_KEY=
# BINANCE_SECRET_KEY=
# JADECAP_ALLOW_LIVE=
